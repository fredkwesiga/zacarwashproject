extends layout 

block content

     body#dashboardbody
        .dashboardheading
            h6(style='font-size: 40px;')  Cars Washed & Amount
        br
        form.search#dashboardsearch(action='/dashboard')
                div.form-row
                    div.col-md-8
                        input.form-control.shadow-lg#datedashboard(type="date" name="searchdate" value=defaultDate)
                    div.col-md-4
                        input.btn.btn-primary.btn-block#dashboardbutton(type="submit")
        br        

        table.expensetable.shadow-lg
            thead.dashboardhead 
                tr
                    th Names 
                    th Number of cars 
                    th Payout Amount 
            tbody
            if washers.length 
                each washer in washers
                    tr.dashboardtr 
                        td #{washer.details[0].fullname}
                        td #{washer.count}
                        td #{washer.totalPayout}
                tr
                    th(colspan=2) Total Payout 
                    th #{new Intl.NumberFormat().format(sumPayout.totalPayoutPerDay)}
